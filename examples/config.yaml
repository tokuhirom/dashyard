site_title: "My Dashyard"
# header_color: "#dc2626"

server:
  session_secret: "change-me-in-production"
  # trusted_proxies:        # IPs/CIDRs trusted to set X-Forwarded-For
  #   - "10.0.0.1"

prometheus:
  url: "http://localhost:9090"
  timeout: 30s

dashboards:
  dir: "examples/dashboards"

# Default user: admin / admin
# Generate a new hash with: go run ./cmd/genhash <password>
users:
  - id: "admin"
    password_hash: "$6$D/BkIQYiHD.cKL4A$pbAApV8cWXOv3hTyITrHNmlWe3FyfIJyM2CVFuJxbmXwDZPIVbcXKJbM2dxmJqG/ZJZBtrt8e9bVxt0d7rQKK."

# OAuth authentication (optional, can coexist with password auth)
# auth:
#   oauth:
#     - provider: github
#       client_id: "your-github-client-id"
#       client_secret: "your-github-client-secret"
#       redirect_url: "http://localhost:8080/auth/github/callback"
#       base_url: "https://ghe.example.com"    # optional: for GitHub Enterprise
#       scopes: ["user:email", "read:org"]
#       allowed_users: ["user1", "user2"]       # optional: restrict by GitHub username
#       allowed_orgs: ["my-org"]                 # optional: restrict by GitHub org membership
