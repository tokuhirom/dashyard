site_title: "My Dashyard"
# header_color: "#dc2626"

server:
  session_secret: "change-me-in-production"
  # trusted_proxies:        # IPs/CIDRs trusted to set X-Forwarded-For
  #   - "10.0.0.1"

prometheus:
  url: "http://localhost:9090"
  timeout: 30s

dashboards:
  dir: "examples/dashboards"

# Default user: admin / admin
# Generate a new hash with: go run ./cmd/genhash <password>
users:
  - id: "admin"
    password_hash: "$6$D/BkIQYiHD.cKL4A$pbAApV8cWXOv3hTyITrHNmlWe3FyfIJyM2CVFuJxbmXwDZPIVbcXKJbM2dxmJqG/ZJZBtrt8e9bVxt0d7rQKK."

# OAuth/OIDC authentication (optional, can coexist with password auth above)
# auth:
#   oauth:
#     provider: github               # "github", "google", or "oidc"
#     client_id: "your-client-id"
#     client_secret: "your-client-secret"
#     redirect_url: "http://localhost:8080/auth/callback"
#     # issuer_url: "https://..."    # Required for "oidc" provider
#     # scopes: ["read:user"]        # Defaults per provider if omitted
#     # allowed_users: ["user1"]     # Optional allowlist
#     # allowed_orgs: ["my-org"]     # Optional (GitHub-specific)
